#! unieinfra.resource: 50GiB
version: '3.8'
services:
  unieinfra:
    image: registry.unieai.com/unieverse-private/vllm.gaudi.072
    runtime: habana
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HUGGING_FACE_HUB_TOKEN}
      - MODEL_NAME=${MODEL_NAME}
      - MODEL_CONTEXT_LENGTH=${MODEL_CONTEXT_LENGTH}
    shm_size: 32gb
    entrypoint: vllm
    command: >
      -c "vllm serve ${MODEL_NAME} --port 35640 --host 0.0.0.0
      --tensor-parallel-size 1 --trust_remote_code
      --enable-prefix-caching --max-model-len ${MODEL_CONTEXT_LENGTH} --enforce-eager"