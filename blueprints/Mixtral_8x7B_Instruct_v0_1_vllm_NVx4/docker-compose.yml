#! unieinfra.resource: 300GiB
version: '3.8'
services:
  unieinfra:
    image: vllm/vllm-openai:v0.9.1
    volumes:
    - "./weight:/weight"
    runtime: nvidia
    environment:
    - NCCL_DEBUG=INFO
    - HUGGING_FACE_HUB_TOKEN=${HUGGING_FACE_HUB_TOKEN}
    shm_size: 32gb
    entrypoint: vllm
    command: [serve, mistralai/Mixtral-8x7B-Instruct-v0.1,
              --port, '8910',
              --host, 0.0.0.0,
              --tensor-parallel-size, '4',
              --served-model-name, Mixtral-8x7B-Instruct-v0.1,
              --trust_remote_code,
              --enable-prefix-caching]
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            capabilities:
            - gpu
